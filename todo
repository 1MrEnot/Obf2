Какие-то левые имена в переменных

Идеи:
    Переобъявлять аргументы функкции с новым названием
    def foo(bar):
        OO0OO0O = bar
        ...


Не робит
    if self.O00OOO000O == "polygon" == O0OO00OOOOO.O00O00O0O0[shapeIndex].O00OOO000O:
    return tuple((t11 * x + t12 * y, t21 * x + t22 * y) for (x, y) in polygon)

    (с разрывом)
    O0O000OO0OO = sorted(x for x in docsdict
          if O0OO0OOOOO.split('.')[1] not in _alias_list)
